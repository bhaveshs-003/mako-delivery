import { PhasePlaceholder } from "@/components/shared/PhasePlaceholder";

export default function TicketsPage() {
  return (
    <PhasePlaceholder
      title="Tickets"
      phase="Phase 4"
      description="Multi-project ticket tracking for product bugs, API changes, and escalations — with SLA burn and RL response flow."
    />
  );
}
