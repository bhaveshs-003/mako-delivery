import { PhasePlaceholder } from "@/components/shared/PhasePlaceholder";

export default function ReportsPage() {
  return (
    <PhasePlaceholder
      title="Reports & Analytics"
      phase="Phase 6"
      description="Delay attribution, timeline variance, post-mortem, resource utilization, and trend analysis — with PDF/CSV export."
    />
  );
}
