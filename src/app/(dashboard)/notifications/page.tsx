import { PhasePlaceholder } from "@/components/shared/PhasePlaceholder";

export default function NotificationsPage() {
  return (
    <PhasePlaceholder
      title="Notifications"
      phase="Phase 7"
      description="In-app notification feed and email dispatch for all 19 notification types."
    />
  );
}
